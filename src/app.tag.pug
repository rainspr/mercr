app
  nav.navbar.navbar-default
    .container
      .navbar-header
        button.navbar-toggle.collapsed(type='button', data-toggle='collapse', data-target='#navbar-collapse', aria-expanded='false')
          span.sr-only Toggle navigation
          span.icon-bar
          span.icon-bar
          span.icon-bar
        |
        a.navbar-brand(href='#') メルストのやつ
      #navbar-collapse.collapse.navbar-collapse
        ul.nav.navbar-nav
          li
            a(href='#') home
          li
            a(href='#pray') お祈り
          //li
            a(href="#rfgen") 勧誘カード
          li
            a(href='#prayj') others
  .container
    #content
      h1

  script.
    var r = route.create()
    r('', function() {
      riot.mount('#content', 'pray')
    })
    r('pray', function() {
      riot.mount('#content', 'pray')
    })
    r('rfgen', function() {
      riot.mount('#content', 'rfgen')
    })
    r('prayj', function() {
      riot.mount('#content', 'prayj')
    })
    r(function() {
      riot.mount('#content', 'pray')
    })
    //r('')は後で'home'にする
    //let rt = route.create()
    //rt(activatetab)

home
  h1 home
